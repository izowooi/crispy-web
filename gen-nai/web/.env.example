# 로컬 개발용 환경변수 예시
# 실제 값은 gen-nai/web/.dev.vars 에 저장 (gitignored)
# 배포 시크릿은: wrangler pages secret put NAI_TOKEN --project-name gennai

NAI_TOKEN=pst-...your-novelai-persistent-api-token...
MIN_INTERVAL_MS=10000
